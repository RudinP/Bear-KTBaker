# Bear KTBaker 프로젝트 지침

## 앱 버전

- 앱 변경을 원격 저장소에 푸시할 때마다 직전 원격 버전에서 patch 버전을 정확히 `0.0.1` 올린다.
- 한 번의 푸시에는 한 번만 올리고, 추가 수정으로 다시 푸시하면 patch 버전을 다시 `0.0.1` 올린다.
- `package.json`, `package-lock.json`의 루트 패키지 버전을 항상 동일하게 유지한다.
- 새 버전의 설치 파일을 만들되 `release/`, `dist/`, `dist-electron/` 산출물은 커밋하지 않는다.
